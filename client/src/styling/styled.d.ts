export interface DefaultTheme {
    colors: {
        sidebarColor: string;
        sidebarHighlight: string;

        // Screens etc.
        backgroundColor: string;

        // Main component pallet
        main: string;
        secondary: string;
        active: string;
        words: string;
    }
}
