import styled from "styled-components";
import {Link} from "react-router-dom";

/**
 * Used as a non-styled link container
 */
const Linkable = styled(Link)`
    text-decoration: none;
`;

export default Linkable;